/**
 * Rotas sob `/aniversario-namoro` onde o layout **não** deve mostrar a barra superior (Voltar ao início).
 * Deixe vazio até indicares exceções; comparação por prefixo (`pathname.startsWith`).
 */
export const ANIVERSARIO_NAMORO_NAV_EXCLUDED_PREFIXES: readonly string[] = [];

